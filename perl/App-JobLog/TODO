complete test suite
remove unused code
refactor repeated code into methods
add CSV output
figure out 'job when' bug where the time you finished keeps advancing
add tag option to last command and resume command so you can see the last task with a particular tag or resume the last task with a particular tag
use Class::XSAccessor to speed things up (assuming the speedup is worth loading the module)
use Carp throughout
investigate display issue where tag column seems to vary in width
make 'tomorrow through January 2' parsable
