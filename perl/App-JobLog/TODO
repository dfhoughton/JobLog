Key
 . = still to do
 - = discarded
 * = completed

. complete test suite
. remove unused code
. refactor repeated code into methods
. add CSV output
. figure out 'job when' bug where the time you finished keeps advancing
. use Class::XSAccessor to speed things up (assuming the speedup is worth loading the module)
. use Carp throughout
. create import and export functions
. improve error handling in edit functionality when an impossible hour has been added for a line
. add "since" to temporal expressions understood
. add mass-tagging/untagging functionality
. for commands like "done" which don't expect any arguments, get them to fail with diagnostic output when they receive arguments
